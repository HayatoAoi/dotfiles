#+TITLE: dotfiles

* Overview
  This is dotfiles.
  Some configuration files are here.
   + shell(=.bashrc=, =.bash_profile=, =.inputrc=...)
   + keymaps(=.Xmodmap=, =*.map=...)
   + Systems(=pacman.conf=...)
   + Applications(=.latexmkrc=, =.emacs.d=...)

